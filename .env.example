# Only needed for the validation scripts (validation/*.mjs).
# The web app itself never reads these — visitors bring their own key via the Settings dialog.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
JUDGE_MODEL=gpt-4o-mini
